name: emerald

services:
  emerald:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - emerald-data:/data
      - emerald-agents:/root/.agents
    environment:
      - EMERALD_PORT=8080
      - EMERALD_DB_PATH=/data/emerald.db
      - EMERALD_SKILLS_DIR=/root/.agents/skills
    restart: unless-stopped

volumes:
  emerald-data:
  emerald-agents:
